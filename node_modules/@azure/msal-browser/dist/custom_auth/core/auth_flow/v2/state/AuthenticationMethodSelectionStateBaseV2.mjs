/*! @azure/msal-browser v5.23.0 2026-09-23 */
'use strict';
import { AuthFlowActionRequiredStateBase } from '../../AuthFlowState.mjs';
import { InvalidArgumentError } from '../../../error/InvalidArgumentError.mjs';

/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License.
 */
class AuthenticationMethodSelectionStateBaseV2 extends AuthFlowActionRequiredStateBase {
    constructor(stateParameters) {
        super(stateParameters);
        this.methods = stateParameters.methods;
    }
    async requestSelectedMethodChallenge(method) {
        const { correlationId, continuationState, flowClient } = this.stateParameters;
        const selectedMethod = this.methods.find((candidate) => candidate.id === method.id);
        if (!selectedMethod) {
            throw new InvalidArgumentError("method", correlationId);
        }
        const result = await flowClient.requestChallenge({
            correlationId,
            continuationState: {
                ...continuationState,
                links: {
                    ...continuationState.links,
                    challenge: selectedMethod.challengeHref,
                },
            },
        });
        return { result, selectedMethod };
    }
}

export { AuthenticationMethodSelectionStateBaseV2 };
//# sourceMappingURL=AuthenticationMethodSelectionStateBaseV2.mjs.map
