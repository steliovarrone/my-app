/*! @azure/msal-browser v5.23.0 2026-09-23 */
'use strict';
import { CustomAuthResultV2 } from '../CustomAuthResultV2.mjs';
import { VerifyChallengeErrorV2 } from '../error/VerifyChallengeErrorV2.mjs';
import { RequestChallengeErrorV2 } from '../error/RequestChallengeErrorV2.mjs';
import { NewPasswordRequiredStateV2 } from '../../../../reset_password/auth_flow/v2/state/NewPasswordRequiredStateV2.mjs';
import { CompletedStateV2 } from './CompletedStateV2.mjs';
import { MFARequiredStateV2 } from './MFARequiredStateV2.mjs';
import { CustomAuthAccountData } from '../../../../get_account/auth_flow/CustomAuthAccountData.mjs';
import { FLOW_NEW_PASSWORD_REQUIRED_V2, FLOW_ATTRIBUTES_REQUIRED_V2, FLOW_SIGN_UP_PASSWORD_REQUIRED_V2, FLOW_SIGN_IN_CONTINUATION_REQUIRED_V2, FLOW_MFA_REQUIRED_V2, FLOW_COMPLETED_V2 } from '../../../interaction_client/v2/result/FlowActionResultV2.mjs';
import { CustomAuthError } from '../../../error/CustomAuthError.mjs';
import { UNSUPPORTED_FLOW_TRANSITION } from '../../../network_client/custom_auth_api/v2/ErrorCodesV2.mjs';
import { SignInContinuationStateV2 } from '../../../../sign_in/auth_flow/v2/state/SignInContinuationStateV2.mjs';
import { ChallengeVerificationStateBaseV2 } from './ChallengeVerificationStateBaseV2.mjs';

/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License.
 */
/**
 * State returned when first-factor email authentication requires a one-time
 * code. It allows the app to submit the code or request a replacement.
 */
class CodeRequiredStateV2 extends ChallengeVerificationStateBaseV2 {
    constructor() {
        super(...arguments);
        this.stateType = "codeRequired";
    }
    /**
     * Submits the first-factor email code and advances the active flow. The
     * result may complete the operation or require another flow-specific step.
     * @param code - The code to submit.
     * @returns The result of submitting the first-factor code.
     */
    async submitCode(code) {
        const { correlationId, logger, continuationState } = this.stateParameters;
        try {
            logger.verbose("1x3xae", correlationId);
            const result = await this.submitCodeCore(code);
            const resultType = result.type;
            if (result.type === FLOW_NEW_PASSWORD_REQUIRED_V2) {
                return new CustomAuthResultV2(new NewPasswordRequiredStateV2({
                    correlationId: result.correlationId,
                    logger,
                    config: this.stateParameters.config,
                    flowClient: this.stateParameters.flowClient,
                    continuationState: result.continuationState,
                    cacheClient: this.stateParameters.cacheClient,
                }), undefined, result.continuationState.scenario);
            }
            if (result.type === FLOW_ATTRIBUTES_REQUIRED_V2 ||
                result.type === FLOW_SIGN_UP_PASSWORD_REQUIRED_V2) {
                const signUpStateTransitionHandler = this.stateParameters.signUpStateTransitionHandler;
                if (!signUpStateTransitionHandler) {
                    throw new CustomAuthError(UNSUPPORTED_FLOW_TRANSITION, "Sign-up state transition handler is missing.", correlationId);
                }
                return signUpStateTransitionHandler.handleVerification(result, {
                    ...this.stateParameters,
                    signUpStateTransitionHandler,
                });
            }
            if (result.type === FLOW_SIGN_IN_CONTINUATION_REQUIRED_V2) {
                return new CustomAuthResultV2(new SignInContinuationStateV2({
                    correlationId: result.correlationId,
                    logger,
                    config: this.stateParameters.config,
                    flowClient: this.stateParameters.flowClient,
                    continuationState: result.continuationState,
                    cacheClient: this.stateParameters.cacheClient,
                }), undefined, result.continuationState.scenario);
            }
            if (result.type === FLOW_MFA_REQUIRED_V2) {
                return new CustomAuthResultV2(new MFARequiredStateV2({
                    correlationId: result.correlationId,
                    logger,
                    config: this.stateParameters.config,
                    flowClient: this.stateParameters.flowClient,
                    continuationState: result.continuationState,
                    cacheClient: this.stateParameters.cacheClient,
                    methods: result.methods,
                }), undefined, result.continuationState.scenario);
            }
            if (result.type === FLOW_COMPLETED_V2) {
                const account = new CustomAuthAccountData(result.authenticationResult.account, this.stateParameters.config, this.stateParameters.cacheClient, logger, correlationId);
                return new CustomAuthResultV2(new CompletedStateV2(), account, continuationState.scenario);
            }
            throw new CustomAuthError(UNSUPPORTED_FLOW_TRANSITION, `Code submission result type '${resultType}' is not supported.`, correlationId);
        }
        catch (error) {
            logger.errorPii(`0br9qy ${error}`, correlationId);
            return CustomAuthResultV2.createWithError(error, {
                errorType: VerifyChallengeErrorV2,
                scenario: continuationState.scenario,
                correlationId,
            });
        }
    }
    /**
     * Requests a replacement first-factor email code. The returned state
     * contains the refreshed challenge metadata.
     * @returns The result of requesting a replacement code.
     */
    async resendCode() {
        const { correlationId, logger, continuationState } = this.stateParameters;
        try {
            logger.verbose("1xub2c", correlationId);
            const result = await this.resendCodeCore();
            return new CustomAuthResultV2(new CodeRequiredStateV2({
                correlationId: result.correlationId,
                logger,
                config: this.stateParameters.config,
                flowClient: this.stateParameters.flowClient,
                continuationState: result.continuationState,
                cacheClient: this.stateParameters.cacheClient,
                signUpStateTransitionHandler: this.stateParameters.signUpStateTransitionHandler,
                method: this.method,
                sentTo: result.sentTo,
                channel: result.channel,
                codeLength: result.codeLength,
            }), undefined, result.continuationState.scenario);
        }
        catch (error) {
            logger.errorPii(`0eq23q ${error}`, correlationId);
            return CustomAuthResultV2.createWithError(error, {
                errorType: RequestChallengeErrorV2,
                scenario: continuationState.scenario,
                correlationId,
            });
        }
    }
}

export { CodeRequiredStateV2 };
//# sourceMappingURL=CodeRequiredStateV2.mjs.map
