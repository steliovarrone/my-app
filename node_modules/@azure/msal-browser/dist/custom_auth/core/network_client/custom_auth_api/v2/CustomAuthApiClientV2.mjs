/*! @azure/msal-browser v5.23.0 2026-09-23 */
'use strict';
import { HttpMethod } from '../../http_client/IHttpClient.mjs';
import { BaseApiClientV2 } from './BaseApiClientV2.mjs';
import { VerifyNextActionV2, AuthenticationFactorV2, ChallengeNextActionV2 } from './result/BaseResultsV2.mjs';
import { SignUpSubmitAttributesNextActionV2 } from './result/SignUpResultsV2.mjs';
import { ResponseStateV2, COLLECT_ATTRIBUTES_RELATION, VERIFY_RELATION, AuthenticationMethodTypeV2, UPDATE_RELATION, CHALLENGE_RELATION, RISK_VERIFY_RELATION } from './ApiClientConstantsV2.mjs';
import { INVALID_HAL_RESPONSE, UNEXPECTED_AUTHENTICATION_FACTOR, NO_AUTHENTICATION_METHODS } from './ErrorCodesV2.mjs';
import { CustomAuthApiError } from '../../../error/CustomAuthApiError.mjs';

/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License.
 */
/*
 * Native Auth V2 network client that follows server-provided HAL links.
 * Flow-specific entry methods reuse shared challenge, verification, and token operations.
 */
class CustomAuthApiClientV2 extends BaseApiClientV2 {
    /*
     * Starts the reset-password flow using a server-provided link.
     */
    async resetPasswordStart(resetPasswordHref, request, context) {
        return this.sendStartRequest(resetPasswordHref, request, context);
    }
    /*
     * Starts sign-in using a server-provided link.
     */
    async signInStart(signInHref, request, context) {
        const result = await this.sendStartRequest(signInHref, request, context);
        return result;
    }
    /*
     * Starts sign-up using a server-provided link.
     */
    async signUpStart(signUpHref, request, context) {
        const parsedResponse = await this.sendActionRequest(signUpHref, HttpMethod.POST, request, context);
        return {
            continuationToken: this.handler.requireContinuationToken(parsedResponse.continuationToken, parsedResponse.correlationId),
            submitAttributesHref: this.handler.requireHref(parsedResponse.body._links?.submitAttributes?.href, "submitAttributes", parsedResponse.correlationId),
            attributes: parsedResponse.body.attributes,
        };
    }
    /*
     * Submits the initial sign-up attributes. A successful response has already
     * sent the email code and supplies the links needed to verify or resend it.
     */
    async submitSignUpAttributes(submitAttributesHref, request, context) {
        const parsedResponse = await this.sendActionRequest(submitAttributesHref, HttpMethod.POST, request, context);
        const correlationId = parsedResponse.correlationId;
        const continuationToken = this.handler.requireContinuationToken(parsedResponse.continuationToken, correlationId);
        if (parsedResponse.body.state === ResponseStateV2.CONTINUE) {
            return {
                nextAction: SignUpSubmitAttributesNextActionV2.CONTINUE,
                continuationToken,
            };
        }
        if (parsedResponse.body.action === COLLECT_ATTRIBUTES_RELATION) {
            return {
                nextAction: SignUpSubmitAttributesNextActionV2.COLLECT_ATTRIBUTES,
                continuationToken,
                attributes: parsedResponse.body.attributes ?? [],
                submitAttributesHref: this.handler.requireHref(parsedResponse.body._links?.submitAttributes?.href, "submitAttributes", correlationId),
            };
        }
        if (parsedResponse.body.action === VERIFY_RELATION) {
            return {
                ...this.toChallengeVerificationResult(parsedResponse),
                attributes: parsedResponse.body.attributes,
            };
        }
        const message = "Invalid HAL response: sign-up attribute submission returned no known next action";
        this.logger?.error(message, correlationId);
        throw new CustomAuthApiError(INVALID_HAL_RESPONSE, message, correlationId);
    }
    /*
     * Requests the selected method's challenge and returns the data needed for verification.
     */
    async requestChallenge(challengeHref, request, context) {
        const parsedResponse = await this.sendActionRequest(challengeHref, HttpMethod.POST, request, context);
        return this.toChallengeResult(parsedResponse);
    }
    async verifyRisk(riskVerifyHref, request, context) {
        const parsedResponse = await this.sendActionRequest(riskVerifyHref, HttpMethod.POST, request, context);
        if (parsedResponse.body.action !== VERIFY_RELATION) {
            throw new CustomAuthApiError(INVALID_HAL_RESPONSE, "Invalid HAL response: SMS risk verification returned no known next action", parsedResponse.correlationId);
        }
        if (parsedResponse.body.type?.toLowerCase() !==
            AuthenticationMethodTypeV2.SMS) {
            throw new CustomAuthApiError(INVALID_HAL_RESPONSE, "Invalid HAL response: SMS risk verification returned a non-SMS challenge", parsedResponse.correlationId);
        }
        return this.toChallengeVerificationResult(parsedResponse);
    }
    /*
     * Verifies the submitted credential and returns the server-directed next action.
     */
    async verifyChallenge(verifyHref, request, context) {
        const parsedResponse = await this.sendActionRequest(verifyHref, HttpMethod.POST, request, context);
        return this.toVerifyResult(parsedResponse);
    }
    /*
     * Submits the new password and returns the link used to poll for completion.
     */
    async submitNewPassword(updateHref, request, context) {
        const parsedResponse = await this.sendActionRequest(updateHref, HttpMethod.PUT, request, context);
        const nextContinuationToken = this.handler.requireContinuationToken(parsedResponse.continuationToken, parsedResponse.correlationId);
        return {
            continuationToken: nextContinuationToken,
            pollHref: this.handler.requireHref(parsedResponse.body._links?.poll?.href, "poll", parsedResponse.correlationId),
        };
    }
    /*
     * Polls once for reset completion and returns the next server-provided link.
     */
    async poll(pollHref, request, context) {
        const parsedResponse = await this.sendActionRequest(pollHref, HttpMethod.POST, request, context);
        const isCompleted = parsedResponse.body.state === ResponseStateV2.CONTINUE;
        return {
            continuationToken: parsedResponse.continuationToken ?? request.continuationToken,
            isCompleted,
            pollHref: isCompleted
                ? undefined
                : this.handler.requireHref(parsedResponse.body._links?.poll?.href, "poll", parsedResponse.correlationId),
        };
    }
    async sendStartRequest(startHref, request, context) {
        const parsedResponse = await this.sendActionRequest(startHref, HttpMethod.POST, request, context);
        const continuationToken = this.handler.requireContinuationToken(parsedResponse.continuationToken, parsedResponse.correlationId);
        return {
            continuationToken,
            methods: this.resolveMethods(parsedResponse.body, parsedResponse.correlationId),
            authenticationFactor: this.resolveAuthenticationFactor(parsedResponse.body.challengeContext?.authenticationFactor, parsedResponse.correlationId),
            scenario: parsedResponse.body.scenario,
        };
    }
    toVerifyResult(parsedResponse) {
        const correlationId = parsedResponse.correlationId;
        const continuationToken = this.handler.requireContinuationToken(parsedResponse.continuationToken, correlationId);
        if (parsedResponse.body.state === ResponseStateV2.CONTINUE) {
            return {
                nextAction: VerifyNextActionV2.CONTINUE,
                continuationToken,
            };
        }
        if (parsedResponse.body.action === COLLECT_ATTRIBUTES_RELATION) {
            return {
                nextAction: VerifyNextActionV2.COLLECT_ATTRIBUTES,
                continuationToken,
                attributes: parsedResponse.body.attributes ?? [],
                submitAttributesHref: this.handler.requireHref(parsedResponse.body._links?.submitAttributes?.href, "submitAttributes", correlationId),
            };
        }
        if (parsedResponse.body.action === UPDATE_RELATION) {
            return {
                nextAction: VerifyNextActionV2.UPDATE,
                continuationToken,
                updateHref: this.handler.requireHref(parsedResponse.body._links?.update?.href, "update", correlationId),
            };
        }
        if (parsedResponse.body.action === CHALLENGE_RELATION) {
            return {
                nextAction: VerifyNextActionV2.CHALLENGE,
                continuationToken,
                authenticationFactor: this.resolveAuthenticationFactor(parsedResponse.body.challengeContext?.authenticationFactor, correlationId),
                methods: this.resolveMethods(parsedResponse.body, correlationId),
            };
        }
        throw new CustomAuthApiError(INVALID_HAL_RESPONSE, "Invalid HAL response: verify returned no known next action", correlationId);
    }
    resolveAuthenticationFactor(authenticationFactor, correlationId) {
        if (authenticationFactor === AuthenticationFactorV2.SINGLE_FACTOR ||
            authenticationFactor === AuthenticationFactorV2.MULTI_FACTOR) {
            return authenticationFactor;
        }
        const message = authenticationFactor
            ? `Unexpected authentication factor '${authenticationFactor}'.`
            : "The response did not include an authentication factor.";
        this.logger?.error(message, correlationId);
        throw new CustomAuthApiError(UNEXPECTED_AUTHENTICATION_FACTOR, message, correlationId);
    }
    toChallengeResult(parsedResponse) {
        if (parsedResponse.body.action === RISK_VERIFY_RELATION) {
            const riskResponse = parsedResponse.body;
            return {
                nextAction: ChallengeNextActionV2.RISK_VERIFY,
                continuationToken: this.handler.requireContinuationToken(parsedResponse.continuationToken, parsedResponse.correlationId),
                riskVerifyHref: this.handler.requireHref(riskResponse._links?.riskverify?.href, RISK_VERIFY_RELATION, parsedResponse.correlationId),
            };
        }
        if (parsedResponse.body.action === VERIFY_RELATION) {
            return this.toChallengeVerificationResult(parsedResponse);
        }
        throw new CustomAuthApiError(INVALID_HAL_RESPONSE, "Invalid HAL response: challenge returned no known next action", parsedResponse.correlationId);
    }
    toChallengeVerificationResult(parsedResponse) {
        const continuationToken = this.handler.requireContinuationToken(parsedResponse.continuationToken, parsedResponse.correlationId);
        const links = parsedResponse.body._links;
        const codeMetadata = "hint" in parsedResponse.body ||
            "codeLength" in parsedResponse.body ||
            "payload" in parsedResponse.body
            ? parsedResponse.body
            : undefined;
        return {
            nextAction: ChallengeNextActionV2.VERIFY,
            continuationToken,
            verifyHref: this.handler.requireHref(links?.verify?.href, "verify", parsedResponse.correlationId),
            resendHref: links && "resend" in links ? links.resend?.href : undefined,
            codeLength: codeMetadata?.codeLength ?? codeMetadata?.payload?.codeLength,
            hint: codeMetadata?.hint,
            type: parsedResponse.body.type,
        };
    }
    resolveMethods(body, correlationId) {
        const methods = this.handler.requireMethods(body._embedded?.methods, correlationId);
        const resolved = [];
        methods.forEach((method, index) => {
            const href = method._links?.challenge?.href;
            if (href) {
                resolved.push({
                    id: method.id ?? method.type ?? `method-${index}`,
                    type: method.type,
                    hint: method.hint,
                    challengeHref: href,
                });
            }
        });
        if (resolved.length === 0) {
            this.logger?.error("1poo9g", correlationId);
            throw new CustomAuthApiError(NO_AUTHENTICATION_METHODS, "The flow-start response contains no authentication method with a challenge link", correlationId);
        }
        return resolved;
    }
}

export { CustomAuthApiClientV2 };
//# sourceMappingURL=CustomAuthApiClientV2.mjs.map
