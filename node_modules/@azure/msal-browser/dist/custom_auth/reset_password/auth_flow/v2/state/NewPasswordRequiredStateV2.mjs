/*! @azure/msal-browser v5.23.0 2026-09-23 */
'use strict';
import { AuthFlowActionRequiredStateBase } from '../../../../core/auth_flow/AuthFlowState.mjs';
import { CustomAuthResultV2 } from '../../../../core/auth_flow/v2/CustomAuthResultV2.mjs';
import { SignInContinuationStateV2 } from '../../../../sign_in/auth_flow/v2/state/SignInContinuationStateV2.mjs';
import { SubmitNewPasswordErrorV2 } from '../error_type/SubmitNewPasswordErrorV2.mjs';

/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License.
 */
/**
 * State returned when the user must supply a new password to complete the flow.
 * A successful submission returns `SignInContinuationStateV2` so the app can
 * sign the user in.
 */
class NewPasswordRequiredStateV2 extends AuthFlowActionRequiredStateBase {
    constructor() {
        super(...arguments);
        this.stateType = "newPasswordRequired";
    }
    /**
     * Submits the new password to complete the reset. The result either allows
     * sign-in or reports that the password was rejected.
     * @param password - The new password to set.
     * @returns The result of submitting the new password.
     */
    async submitNewPassword(password) {
        const { correlationId, logger, continuationState, flowClient } = this.stateParameters;
        try {
            this.ensurePasswordIsNotEmpty(password);
            logger.verbose("0ie1qa", correlationId);
            const result = await flowClient.submitNewPassword({
                correlationId,
                continuationState,
                newPassword: password,
            });
            return new CustomAuthResultV2(new SignInContinuationStateV2({
                correlationId: result.correlationId,
                logger,
                config: this.stateParameters.config,
                flowClient,
                continuationState: result.continuationState,
                cacheClient: this.stateParameters.cacheClient,
            }), undefined, result.continuationState.scenario);
        }
        catch (error) {
            logger.errorPii(`03m081 ${error}`, correlationId);
            return CustomAuthResultV2.createWithError(error, {
                errorType: SubmitNewPasswordErrorV2,
                scenario: continuationState.scenario,
                correlationId,
            });
        }
    }
}

export { NewPasswordRequiredStateV2 };
//# sourceMappingURL=NewPasswordRequiredStateV2.mjs.map
