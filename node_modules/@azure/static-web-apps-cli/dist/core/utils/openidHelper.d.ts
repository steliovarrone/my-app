import * as client from "openid-client";
export declare class OpenIdHelper {
    private issuerUrl;
    private clientId;
    constructor(issuerUrl: string, clientId: string);
    /**
     * Discover issuer metadata from the OpenID Connect provider
     */
    discoverIssuer(): Promise<client.Configuration>;
    /**
     * Retrieve the authorization endpoint from the issuer
     */
    getAuthorizationEndpoint(): Promise<string>;
}
//# sourceMappingURL=openidHelper.d.ts.map